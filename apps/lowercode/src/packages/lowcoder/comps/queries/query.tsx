import { QueryConfigLabel } from "lowcoder-design";
import { trans } from "#lowcoder/i18n";

export const QueryConfigLabelMethod = () => {
  return <QueryConfigLabel>{trans("query.method")}</QueryConfigLabel>;
};
