import type { Meta } from '@storybook/react'

const meta: Meta = {
  title: 'Unstyled / Providers / Environment',
}

export default meta

export { Basic } from './examples/basic'
export { ShadowRoot } from './examples/shadow-root'
