import type { Meta } from '@storybook/react'

const meta: Meta = {
  title: 'Unstyled / Components / ClientOnly',
}

export default meta

export { Basic } from './examples/basic'
export { WithFallback } from './examples/with-fallback'
