import type { Meta } from '@storybook/react-vite'

const meta: Meta = {
  title: 'UI / Unstyled / Providers / Environment',
}

export default meta

export { Basic } from './examples/basic'
export { ShadowRoot } from './examples/shadow-root'
