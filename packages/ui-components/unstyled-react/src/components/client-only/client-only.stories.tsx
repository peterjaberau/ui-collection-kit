import type { Meta } from '@storybook/react-vite'

const meta: Meta = {
  title: 'UI / Unstyled / ClientOnly',
}

export default meta

export { Basic } from './examples/basic'
export { WithFallback } from './examples/with-fallback'
